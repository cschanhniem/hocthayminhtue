<!DOCTYPE html>
<html lang="vi">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=5.0">
    <meta http-equiv="Content-Security-Policy" content="default-src 'self' https://fonts.googleapis.com https://fonts.gstatic.com; style-src 'self' 'unsafe-inline' https://fonts.googleapis.com; script-src 'self' 'unsafe-inline';">
    <meta name="description" content="{% if description %}{{ description }}{% else %}Tìm hiểu lý thuyết Phật giáo từ kinh Nikaya với hướng dẫn chi tiết và dễ hiểu{% endif %}">
    <title>{% if title %}{{ title }} - {% endif %}Lý Thuyết Phật Giáo từ Kinh Nikaya</title>
    <link rel="stylesheet" href="/css/style.css" />
    <link rel="icon" href="/images/logo.svg" type="image/svg+xml" />
    <link href="https://fonts.googleapis.com/css2?family=Montserrat:wght@400;500;600;700&family=Inter:wght@400;500&family=EB+Garamond:ital@0;1&display=swap" rel="stylesheet">
</head>
<body class="min-h-screen bg-bg-base text-text-base aurora-bg">
    <header class="glass-nav sticky top-0 z-50 mb-8">
        <div class="container mx-auto px-4 py-4 flex justify-between items-center">
            <h1 class="text-xl m-0 flex items-center flex-nowrap">
                <img src="/images/logo.svg" alt="Logo" class="h-6 w-6 mr-2 flex-shrink-0" />
                <a href="/" class="text-primary hover:text-secondary no-underline font-montserrat font-bold truncate">Lý Thuyết Phật Giáo từ Kinh Nikaya</a>
            </h1>
            <button id="menu-toggle" class="md:hidden bg-transparent border-0 text-primary text-2xl cursor-pointer ml-2" aria-label="Toggle menu">☰</button>
        </div>
    </header>

    <div id="menu-backdrop" class="fixed inset-0 bg-black transition-opacity duration-300 ease-in-out opacity-0 z-40 hidden md:hidden" aria-hidden="true"></div>

    <div class="container mx-auto px-4 py-8">
        <div class="flex flex-col md:flex-row gap-6">
            <nav class="sidebar fixed md:static w-full md:w-72 glass-card md:block hidden z-50 top-[72px] left-0 right-0 bottom-0 overflow-y-auto transition-transform duration-300 transform p-4 md:p-0" id="sidebar">
                <div class="tree-container">
                    {% include "partials/tree.njk" %}
                </div>
            </nav>
            
            <main class="flex-1">
                <article class="concept-card glass-card prose prose-stone max-w-none">
                    {{ content | safe }}
                </article>

                {% if concept and concept.children and concept.children.length > 0 %}
                <div class="glass-card mt-8">
                    <h3 class="text-xl font-semibold text-primary mb-4">Các khái niệm liên quan</h3>
                    <ul class="space-y-2">
                        {% for child in concept.children %}
                        <li>
                            <a href="/concepts/{{ child.id }}/" class="nav-item">{{ child.label }}</a>
                        </li>
                        {% endfor %}
                    </ul>
                </div>
                {% endif %}
            </main>
        </div>
    </div>

    <footer class="glass-nav mt-8 py-6">
        <div class="container mx-auto px-4 text-center">
            <p class="text-sm text-gray-600">Dự án cộng đồng chia sẻ lý thuyết Phật giáo từ kinh Nikaya</p>
        </div>
    </footer>

    <script src="/js/main.js"></script>
</body>
</html>
