<!DOCTYPE html>
<html lang="vi">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta name="description" content="{% if description %}{{ description }}{% else %}Tìm hiểu lý thuyết Phật giáo từ kinh Nikaya với hướng dẫn chi tiết và dễ hiểu{% endif %}">
    <title>{% if title %}{{ title }} - {% endif %}Lý Thuyết Phật Giáo từ Kinh Nikaya</title>
    <link rel="icon" href="/images/logo.svg" type="image/svg+xml">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/@picocss/pico@2/css/pico.min.css">
    <link href="/css/style.css" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Montserrat:wght@400;500;600;700&family=Inter:wght@400;500&family=EB+Garamond:ital@0;1&display=swap" rel="stylesheet">
</head>
<body>
    <nav class="container-fluid">
        <ul>
            <li>
                <a href="/" class="logo">
                    <img src="/images/logo.svg" alt="Logo" width="32" height="32">
                    <strong>Lý Thuyết Phật Giáo từ Kinh Nikaya</strong>
                </a>
            </li>
        </ul>
        <ul>
            <li><button id="menu-toggle" class="outline menu-button" aria-label="Menu">☰</button></li>
        </ul>
    </nav>

    <main class="container">
        <div class="grid">
            <aside id="sidebar" class="sidebar">
                <div class="tree">
                    {% include "partials/tree.njk" %}
                </div>
            </aside>
            
            <div class="content">
                <article>
                    {{ content | safe }}
                </article>

                {% if concept and concept.children and concept.children.length > 0 %}
                <section class="related-concepts">
                    <h3>Các khái niệm liên quan</h3>
                    <ul>
                        {% for child in concept.children %}
                        <li>
                            <a href="/concepts/{{ child.id }}/">{{ child.label }}</a>
                        </li>
                        {% endfor %}
                    </ul>
                </section>
                {% endif %}
            </div>
        </div>
    </main>

    <footer class="container">
        <p>Dự án cộng đồng chia sẻ lý thuyết Phật giáo từ kinh Nikaya</p>
    </footer>

    <script src="/js/main.js"></script>
</body>
</html>
